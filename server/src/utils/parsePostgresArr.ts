// ? пока нет нужды
// import { parse } from 'postgres-array';

// export default function(postgresArr: string): Array<any> {
//     console.log(postgresArr);
//     if (!postgresArr) return [];

//     return parse(postgresArr, val => val);
// }
